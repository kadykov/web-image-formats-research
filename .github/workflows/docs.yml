# Build and deploy documentation to GitHub Pages.
#
# Builds the Astro Starlight documentation site and deploys it to the
# docs/ subdirectory on the gh-pages branch.  The landing page is also
# refreshed on each deploy.
#
# This workflow runs independently of the study workflow — updating
# documentation does not affect the research report, and vice versa.
#
# Prerequisites:
#   Repository Settings → Pages → Source must be set to
#   "Deploy from a branch" with branch "gh-pages" and folder "/ (root)".

name: Deploy Docs

on:
  push:
    branches: [main]
    paths:
      - "config/site.json"
      - "docs/**"
      - "docs-site/**"
      - "src/**"
      - "pages/**"
      - "scripts/generate_docs.py"
      - "scripts/stage_github_pages.py"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

concurrency:
  group: docs-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    name: Build & Deploy Documentation
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: docs-site/package-lock.json

      - name: Install Python dependencies
        run: pip install -e ".[dev]"

      - name: Install docs-site dependencies
        run: cd docs-site && npm ci

      - name: Generate documentation content
        run: python3 scripts/generate_docs.py

      - name: Build documentation site
        run: cd docs-site && npm run build

      - name: Load current gh-pages tree
        run: |
          if git ls-remote --exit-code --heads origin gh-pages >/dev/null 2>&1; then
            git fetch origin gh-pages
            git worktree add --detach site-current origin/gh-pages
          else
            mkdir -p site-current
          fi

      - name: Stage combined site
        run: |
          python3 scripts/stage_github_pages.py \
            --output-root site-staged \
            --current-root site-current \
            --landing-root pages \
            --docs-root docs-site/dist

      - name: Deploy staged site root
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site-staged
